# Book API

A simple REST API for managing books built with FastAPI and SQLite.

## Description

This API allows you to create, read, update, delete, and search books. Each book has the following fields:

- `id`: integer, unique identifier (auto-generated)
- `title`: string, the title of the book
- `author`: string, the author of the book
- `year`: integer, publication year (optional)
